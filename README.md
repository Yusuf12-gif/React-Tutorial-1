# React + Vite

This is a React project where it is all about basic features of repo like props, components, hooks and everything . There is a dummy js file which contains data to be fetched and displayed on screen using all these features.
